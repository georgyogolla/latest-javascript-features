// Works like the normal []. used to access elements in an array using their indices but at()supports negative indices.

const arr = [1, 2, 3, 4, 5];

console.log(arr.at(0)); // Output: 1
console.log(arr.at(-1)); // Output: 5